import { UserDataType } from '../../../interfaces/user.interfaces';

export function sortingList(
  list: UserDataType[],
  type: string,
): UserDataType[] {
  const newList: UserDataType[] = [];

  return newList;
}
